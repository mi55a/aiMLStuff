import numpy as np
inport matplotlib.pyplot as plt
import sys
# vectorized calculation: mathematical operations performed on a set of values at once, rather than individually
# having an algorithm work on a vector
# linearly spaced array in numpy array[start, stop, how many values]

array = np.linspace(2,10,5)
# if using a for loop in python, you're doing it wrong

